---
feature_ids: [1, 7]
topics: [acceptance, g2, device, evidence, exact-build, hook]
doc_kind: evidence
created: 2026-08-26
device_serial4: ZY22
exact_head: e20a24a0f434d7f58d30102fe0bfa686173c7b7f
status: blocked
---

# G2 场次 S1 独立记录（ZY22）

## 记录结论

**S1 未达到 runner readiness，按硬中止点停在长跑之前。** 这不是 G2 verdict，也不授权
`--cellular-matrix`、10 地址长跑、ledger 铸行或任何放行操作。

| 块 | 记录结果 | 结论依据 |
|---|---|---|
| S1-A exact-build preflight | **PASS** | 两只 artifact 与设备 `base.apk` 逐字相同，package/version/signer 均从同一字节独立重算 |
| S1-B Hook runner readiness | **FAIL / NOT_READY** | 实际只启动 `.ui.ComposeActivity`；未运行时证明 `.probe.HookAcceptanceActivity`，且 runner exit=1 |
| S1-C §3.2 采集 | **RECORDED / BLOCKED** | 设备实际值已采集，但 mock app 与 LSPosed module/scope 尚无 operator 明确指定 |
| §4.3 checksum 完整性 | **INCOMPLETE** | manifest 内 15/15 通过，但目录中另有未列入的原始 `lspd-modules_config.db-shm` |

执行者为 `@fable-5`，独立记录者为 `@codex-sol`。记录者只记原始事实与 predicate，
不兼任 evidence validity reviewer、`DUAL` signer 或最终放行人。

## 候选与来源

- candidate：`e20a24a0f434d7f58d30102fe0bfa686173c7b7f`
- tree：`652e7db687b167265b65856991e0f5bbbaedb8eb`
- parent/base anchor：`3e05db9e040bd59ecc6ac9bf100d483edb0be895`
- GitHub Actions run：`32982300057`，7 个 check-run 均为 `completed/success`
- 本地复核时：`main == origin/main == candidate`，worktree clean
- 原始目录：`/Users/terry/Desktop/coding/g2-s1-evidence-20260826/`
- 执行时间窗：2026-08-26T15:19:38Z 至约 15:26Z

## 原始证据与 digest

`s1-manifest.sha256` 的 15 个条目由记录者重新执行
`shasum -a 256 -c`，15/15 通过。manifest SHA-256 为：

```text
f46c7d635f2f402b13d50be5563d1cd3756107e3ed413f9856598c7ea55b8612
```

记录者按下列固定顺序直接串接原始字节，得到三个 block report digest：

| 块 | 串接顺序 | reportDigest |
|---|---|---|
| S1-A | build identity → provenance note → installed identity → bench APK → auto APK | `7e916195d6d55e45a33d5fa714abf2a8b45b47a4b53c4a0a62f9d9fbd55cfb6f` |
| S1-B | runner output → component attribution → process/bytes chain | `f618027b8c5912b4fb3dc0407d866012100532e6514f02b5fce4dd871d439067` |
| S1-C | device snapshot → LSPosed probe/scope → DB/WAL → repose | `cd9f1ea64223e614e0841443aa8c22607acc41a360a704a281ca8c32bd186c5b` |

### Manifest 覆盖缺口

目录实际有 18 个文件：15 个 manifest 条目、manifest 自身、校验输出，以及
`lspd-modules_config.db-shm`。最后一项是设备侧 SQLite 快照的一部分，大小 32768 bytes，
在 manifest 生成前已存在，却未被列入。因此不能把“15/15 通过”表述为“目录全部原始文件均被
manifest 覆盖”。

记录者用 `sqlite3 -readonly` 复核 manifest-covered DB 时，该未覆盖的 SHM 辅助文件 mtime 从
18:20:45 EEST 更新到 18:30:39 EEST；manifest 覆盖的 DB/WAL/文本文件未改变，15/15 校验仍通过。
此 SHM 不得再作为本场证据使用，也不得倒填进旧 manifest。后续若补包，应建立新目录并重新封存，
不能修改本目录伪装成原始冻结包。

## S1-A — exact-build preflight

### 字节、身份与 signer

| 包 | version | artifact SHA-256 | frozen device SHA-256 | signer SHA-256 | 判定 |
|---|---|---|---|---|---|
| `name.caiyao.fakegps.bench` | `8` / `3.0.0` | `da508aa83d28ae6adc32945c723151b0af99e8aac7b6cd9fb40c9b4846b9cf46` | 同左 | `7a598cbe6fb816ba74f01b58e3f43b8ff0f463989157e590ebd86c89b53f7e41` | MATCH |
| `com.example.cellrebelauto` | `1` / `1.0` | `ddf5fbd577a0b07a9d1d350b8d11faeed31e7549827a2f9947376d51de915999` | 同左 | `7a598cbe6fb816ba74f01b58e3f43b8ff0f463989157e590ebd86c89b53f7e41` | MATCH |

两组 artifact/device 文件均通过 `cmp`。记录者使用 Android build-tools 36.1.0 的 `apksigner`
和 `aapt` 对四份文件独立重算，四份 signer 均为 `C=US, O=Android, CN=Android Debug`，package 与
version 也与表中一致。`pm path` 原始输出对每包各给出唯一 `base.apk`。

执行包中的首次 artifact signer 命令因未找到 Java 而失败；该失败原文保留在
`s1a-build-identity.txt`，不能伪报为当时成功。记录者随后在相同 artifact/device 字节上独立补算，
因此 exact-build signer claim 可以闭合，但原始命令失败仍是证据包事实。

`contracts/environment-control-v1/compatibility.yaml` 的 SHA-256 为：

```text
c64dd132418493ba5918d86e481382d29b3d351867f3e3d3569577abb3d6f543
```

记录者续证的可复现构建环境为 Gradle 9.3.1、Kotlin 2.2.21、JDK 21.0.10、macOS 26.5.2
aarch64；两只工程的 wrapper 输出一致。原始包没有保存完整 Gradle 命令 stdout/stderr，只在
`s1a-build-identity.txt` 记录 `:app:assembleDebug exit=0 (both)`；这一缺口计入 §4.3，不反转
已经由同字节比较证明的 `WRONG_BUILD` 阴性结论。

## S1-B — Hook runner readiness

### 已证明的部分

冻结 bench APK 的 manifest 中确有：

- `name.caiyao.fakegps.probe.HookAcceptanceActivity`；
- signature permission
  `name.caiyao.fakegps.bench.permission.RUN_HOOK_ACCEPTANCE`；
- bench authority `name.caiyao.fakegps.bench.data.AppInfoProvider`。

本次 `test-hook.sh --current-profile` 启动的 `ComposeActivity` 也能绑定到实装字节：

```text
topResumedActivity =
  name.caiyao.fakegps.bench/name.caiyao.fakegps.ui.ComposeActivity
pid = 7246
/proc/7246/maps base.apk directory == pm path directory
pm path bytes SHA-256 = da508aa83d28ae6adc32945c723151b0af99e8aac7b6cd9fb40c9b4846b9cf46
```

这能证明 runner 的普通 `.bench` package 坐标不是 production 包或短类名假坐标。

### 未证明的承重 predicate

派单要求真机证明 runner 启动的是实装
`name.caiyao.fakegps.bench/name.caiyao.fakegps.probe.HookAcceptanceActivity`。本次没有该 Activity
的 `topResumedActivity`、pid、`/proc/<pid>/maps` 或 session/result 证据；`--current-profile`
按脚本原文只启动 `ACT=...ui.ComposeActivity`，而 `HookAcceptanceActivity` 由
`--cellular-matrix` 的 scenario/recovery 路径启动。

runner 原始终态为：

```text
VERIFIED preflight.device api=35 awake unlocked rooted
HARNESS_ERROR no Xposed prefs-loaded diagnostic
RUNNER-EXIT=1
```

因此 S1-B 只能记为 **FAIL / NOT_READY**，不能把“普通 ComposeActivity 的进程字节归因成立”
改写成“Hook acceptance component readiness 已通过”。这不是 product finding：S1-C 同时显示 bench
LSPosed module `enabled=0`，所需 operator policy 前置尚未成立。执行侧按硬中止点未进入完整 matrix
或 10 地址长跑，处置正确。

## S1-C — §3.2 只读采集

| 项 | 冻结读回 |
|---|---|
| serial / model | `ZY22JHW9M4` / `moto g54 5G` |
| OS / API | Android 15 / API 35 |
| device count | 1 |
| build fingerprint | `motorola/cancunf_g_sysenq/cancunf:15/V1TDS35H.83-20-5-8-4/d3b29e-8d7d82:user/release-keys` |
| mock app-op allow | `com.hopefactory2021.fakegpslocation` 与 `name.caiyao.fakegps.bench` |
| production app-op | `name.caiyao.fakegps`: deny |
| 场次开始 mock provider | `gps provider [mock]:` 存在 |
| 场次结束 mock provider | 无匹配行，`mock-grep exit=1` |
| LSPosed enabled module | `name.caiyao.fakegps`，scope 7 apps |
| bench module | 已登记，`enabled=0`，无 scope row |

记录者对 manifest-covered `lspd-modules_config.db` 做只读 SQL 复核，modules/scope 行与
`s1c-lsposed-scope.txt` 一致。设备侧没有发生记录者写操作。

开始时 hopefactory 在 919ms 前刚活动且 mock provider 存在，结束时 provider 消失；执行者声明未操作
hopefactory，但包内没有能独立归因该状态变化的事件日志。须由 operator 确认是否在场次中关闭了
mock。无论归因如何，结束快照记录为无 mock provider。

production 包在设备 package 列表中存在。本包有两次 streamed install `Success`，摘要称它们分别为
bench 与 Auto；因未保存完整命令行 transcript，只能记录“没有 production mutation 的正面迹象”，
不能从摘要单独证明完整的负面命题。production 未被本记录者安装、替换或清数据。

## 后续门

恢复 S1-B 前需要：

1. operator 明确指定本 G2 场次的 mock-location app；
2. operator 明确指定 LSPosed module 与 scope，并使 bench self-hook 前置可观测；
3. 新场次运行时证明 `HookAcceptanceActivity` 绑定到本次实装 bench 字节；
4. 建立新证据目录，保存完整命令、stdout/stderr、exit code，并让 manifest 覆盖全部原始文件；
5. 由 `@codex-terra` 独立判 evidence validity；若后续全包满足，再由 `@codex-luna` 承担 `DUAL`
   前置 verdict。两者均不由本记录者代签。

`docs/acceptance/matrix-evidence-device.json` 继续保持 `[]`；本场不铸 M-* 行，不改变 Issue #1
`CURRENT-TRUTH`，也不作 G2 放行。
