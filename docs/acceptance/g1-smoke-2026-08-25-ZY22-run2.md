---
feature_ids: [7]
topics: [acceptance, smoke, device, evidence, environment-control-v1]
doc_kind: evidence
created: 2026-08-25
device_serial4: ZY22
run: 2
exact_head: fab6de9cc8c7c6c6e2a86276a42271376e6d44c9
status: passed
---

# G1 冒烟证据 — C5 run 2（ZY22）

## 独立判定

**C5 PASS。** 本轮在 `fab6de9cc8c7c6c6e2a86276a42271376e6d44c9` 上满足
runbook §7.1–§7.5 与 §12 的全部 C5 退出标准。

这只判定一次 G1 冒烟执行通过；它**不等于 G1 达标，不放行 G2，也不覆盖 Auto 主界面启动**。
执行猫为 `@fable-5`，独立记录猫为 `@codex-sol`。

## 原始证据与完整性

- 原始目录：`/Users/terry/Desktop/coding/c5-evidence-run2-20260825/`
- 执行时间：2026-08-25 15:54:34–15:54:49 EEST
- `evidence-manifest.sha256`：22/22 条目通过 `shasum -a 256 -c`
- manifest SHA-256：`8f4fc8cc4e2588fb035daf940bcee97e899304d0260631f6e1b980167ccf39f3`
- `reportDigest: sha256:46ca3a92453a448a987dca18a73fee1a14e64c1a0da22528e7a4477c309e27b2`

`reportDigest` 对下列原始字节按步骤顺序、每步 log 后接 screenshot 直接串接后计算；记录猫未修改
任何原始文件：

```text
run-6.0-seed.log      + run2-6.0-seed.png
run-6.1-handshake.log + run2-6.1-handshake.png
run-6.2a-pairing.log  + run2-6.2a-pairing.png
run-6.2b-approve.log  + run2-6.2b-approve.png
run-6.3-handshake.log + run2-6.3-handshake.png
run-6.4-loop.log      + run2-6.4-loop.png
run-6.5-stop.log      + run2-6.5-stop.png
```

## §11 记录基线

### 1. 设备

| 字段 | 值 |
|---|---|
| 品牌 | motorola |
| 型号 | moto g54 5G |
| Android / API | 15 / 35 |
| serial（前 4） | `ZY22`（完整设备回读为 `ZY22JHW9M4`） |
| build fingerprint | `motorola/cancunf_g_sysenq/cancunf:15/V1TDS35H.83-20-5-8-4/d3b29e-8d7d82:user/release-keys` |

设备字段由记录猫于 2026-08-25 16:06 EEST 只读续证；同次续证再次确认两只实装 APK 的 full SHA
仍与冻结证据及构建产物逐字一致，因此版本字段绑定的是本轮同一构建字节。

### 2–3. 构建、版本与实装身份

两只 APK 均来自同一 checkout：
`fab6de9cc8c7c6c6e2a86276a42271376e6d44c9`。

| 包 | versionCode / versionName | 构建 SHA-256 | 实装 SHA-256 | 判定 |
|---|---|---|---|---|
| `name.caiyao.fakegps.bench` | `8` / `3.0.0` | `8668c5ea56119f9383e73b97663ae639ce05334ae110f6069c86213c425ff222` | 同左 | MATCH |
| `com.example.cellrebelauto` | `1` / `1.0` | `ddf5fbd577a0b07a9d1d350b8d11faeed31e7549827a2f9947376d51de915999` | 同左 | MATCH |

每包 `pm path` 均只有一行 `base.apk`。记录猫只读续证时再次从设备流式计算出同一 full SHA。

### 4. 精确批准主元

- applicationId：`com.example.cellrebelauto`
- signerDigest：`7a598cbe6fb816ba74f01b58e3f43b8ff0f463989157e590ebd86c89b53f7e41`
- approve 参数：

```text
-n name.caiyao.fakegps.bench/name.caiyao.fakegps.integration.v1.PairingApprovalActivity
--es approve_application_id com.example.cellrebelauto
--es approve_signer_digest 7a598cbe6fb816ba74f01b58e3f43b8ff0f463989157e590ebd86c89b53f7e41
```

参数由冻结的 `run-chain.sh` 与 `run-6.2a-pairing.log` 交叉重建；批准结果原文为
`APPROVED: com.example.cellrebelauto`。

### 5. 种子基线

`prepare_kyiv`：Kyiv `50.4501, 30.5234`，altitude `179.0`；
`run-6.0-seed.log` 原文含 `READY command=prepare_kyiv`。

### 6–7. 每步日志与截图

七步 logcat 全文和七张 PNG 均在 manifest 内并通过逐文件校验。七张 PNG 均为有效的
1080×2400 RGBA 文件，但设备全程 Dozing，七张图字节完全相同且视觉内容全黑；它们没有提供
额外 UI 诊断价值。§7 的逐字判据由对应的完整 logcat 提供，本报告不把黑屏截图描述成可视化 PASS。

### 8. apply / observe

```text
[3] apply → lease=4debb81a… rev=3 verif=3
    intentHash=d8a63bcf97223429…
[4] observe → rev=3 coverage=1 mode=null
    fingerprint=intent:50.45009994506836,30.523399353027344@1782432739:verified=false
    hashMatch=true
```

`verif=3` 按当前 runbook 表示发布事务为 `NONE`，照实记录；§7.4 明确不以该值判 harness FAIL。

### 9. 失败/安全终态原文

七份步骤日志中未出现 `TIMEOUT after`、`ABORTED by signal`、`FAILED:`、`STOP:`、
`SAFETY:`、`CLEANUP UNSAFE` 或 `PROTOCOL SKEW`。

### 10. 冒烟后 mock 状态

- `chain-status.txt`：`mock-grep exit=1 (must be 1 = no output)`
- `mock-state-after.txt` 中无 `provider [mock]` 行；保留的 app-op 原文为
  `MOCK_LOCATION: allow; time=+1s399ms ago`
- 记录猫只读续证时再次确认 `dumpsys location` 无 `provider [mock]` 行；bench app-op 仍为 allow。

app-op 的 allow 仅表示 bench 有能力进入 mock，不等于系统当前存在 mock provider；§7.5 判据检查的是
后者，本轮为空。

### 11. capture_step 退出码

| 步骤 | exit |
|---|---:|
| 6.0 seed | 0 |
| 6.1 首次握手 | 0 |
| 6.2a pending 列表 | 0 |
| 6.2b 精确批准 | 0 |
| 6.3 批准后握手 | 0 |
| 6.4 full loop | 0 |
| 6.5 stop | 0 |

退出码来自冻结的 `chain-status.txt`，且与每份日志的终态一致；没有 exit 1/130。

## §7 / §12 逐项裁决

| 判据 | 原始证据 | 裁决 |
|---|---|---|
| §7.1a seed | `READY command=prepare_kyiv` | PASS |
| §7.1b reset 后 discover | `[1] discover → item=profile-1 ver=1 rev=2` | PASS |
| §7.2a pending | `pending callers (1)` + exact appId + 64-hex digest | PASS |
| §7.2b approve | `APPROVED: com.example.cellrebelauto` | PASS |
| §7.3 fail-closed / 握手 | 首次 `REFUSED`；批准后 `CONNECTED`、protocol 1、无 SKEW | PASS |
| §7.4 loop | hashMatch=true；release complete/residuals=[]；advance outcome=2；EXHAUSTED 四腿一致 | PASS |
| §7.5 cleanup | `READY command=stop` + mock provider 零命中 | PASS |

`[7b]` 与终态四腿均为：schedule `qwy-default-schedule`、item `profile-1`、version `2`、
`exhausted=true`。

## 偏离与范围外事实

1. bench 在执行前采用 uninstall + fresh install，而非 §5 默认 `-r`。设备上的同名包签名与本次
   构建不兼容，无法原位覆盖；新装后重新授予 mock app-op、重新 seed，并完整走
   `REFUSED → pending → APPROVED → CONNECTED`，没有复用旧配对状态。
2. `run-chain.sh` 在探针步骤之间没有加入上轮的额外 force-stop workaround；只执行 runbook
   §6.0.1 要求的 seed 后 force-stop。
3. `run-6.2b-approve.log` 能证明批准成功，但执行者所述 `am start` 的
   “delivered to currently running top-most instance” 警告未进入冻结证据包，因此本报告不把该警告
   作为独立封证事实。F-11 的当前 C5 证据限于：无额外 workaround 的连续流程已通过。
4. `f16-main-launch.log` 在冒烟后记录 Auto 主界面仍因 Room identity hash 不一致崩溃：
   expected `0d083aef0412f6d2ad3bbce31bf37f98`，found `dea7bb1231570ea9fab363e19fc3c9b3`。
   主界面启动不在本次 C5 范围内，故不反转 C5 PASS；它仍是独立产品缺陷，不能被本次绿灯掩盖。
