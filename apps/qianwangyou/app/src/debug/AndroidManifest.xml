<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only operator pairing surface (§6.5).
  exported=true so adb can drive it; src/debug so it cannot ship.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="name.caiyao.fakegps.integration.v1.PairingApprovalActivity"
            android:exported="true"
            android:label="EC v1 pairing approval" />
    </application>
</manifest>
